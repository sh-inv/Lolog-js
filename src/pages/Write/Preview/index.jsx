import styled from 'styled-components';

const Preview = () => {
  return <PreviewContainer className='preview-container'></PreviewContainer>;
};

const PreviewContainer = styled.div`
  background-color: white;
`;

export default Preview;
